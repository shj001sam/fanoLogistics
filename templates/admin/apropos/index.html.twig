{% extends 'admin/base.html.twig' %}

{% block title %}Acceuil{% endblock %}

{% block body %}

<!-- banner -->
{% for part in apropos %}


    <div class="container">
        
        
        <h1>{{part.getDescTitle()}}</h1>
        <p>{{part.getDescText()|raw}}</p>
        <br>
        <br>
        <br>
        <a href="{{ path('modif_apropos', {"id": part.getId()}) }}" class="btn btn-primary">Editer</a>
      </div>


{% endfor %}
{% endblock %}