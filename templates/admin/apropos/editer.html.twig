{% extends 'admin/base.html.twig' %}

{% block title %}Editer Apropos{% endblock %}

{% block body %}
 {{form_start(form)}}

       {{ include('admin/apropos/form.html.twig') }}
        <button class="btn btn-primary">
            Editer
        </button>
    {{ form_end(form) }}

{% endblock %}
